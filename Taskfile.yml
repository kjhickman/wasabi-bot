version: '3'

dotenv: ['.env']

tasks:
  default: task --list
  
  build:
    desc: Build the Docker image
    cmds:
      - docker build -t ${AWS_ACCOUNT_ID}.dkr.ecr.us-east-1.amazonaws.com/wasabi-bot:${ENVIRONMENT} . -f src/WasabiBot/Dockerfile

  push:
    desc: Push the image to ECR
    deps: [build]
    cmds:
      - docker push ${AWS_ACCOUNT_ID}.dkr.ecr.us-east-1.amazonaws.com/wasabi-bot:${ENVIRONMENT}

  restore:
    desc: Restore assets for CDKTF
    cmds:
      - dotnet restore src/WasabiBot.Terraform
  
  deploy:
      desc: Deploy via CDKTF
      deps: [push, restore]
      dir: src/WasabiBot.Terraform
      cmds:
        - cdktf deploy --auto-approve

  auth:
    desc: Authenticate with ECR
    cmds:
      - aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin ${AWS_ACCOUNT_ID}.dkr.ecr.us-east-1.amazonaws.com
