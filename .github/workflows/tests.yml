name: Tests

on:
  workflow_call:
    inputs:
      dotnet-version:
        description: .NET SDK version to install
        required: false
        default: 10.0.x
        type: string

permissions:
  contents: read

jobs:
  tests:
    name: Run ${{ matrix.suite }} tests
    runs-on: ubuntu-24.04-arm
    strategy:
      fail-fast: false
      matrix:
        suite: [unit, integration]
    env:
      DOTNET_VERSION: ${{ inputs.dotnet-version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 1

      - name: Setup .NET
        uses: fast-actions/setup-dotnet@v1
        with:
          sdk-version: ${{ env.DOTNET_VERSION }}

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Restore
        run: dotnet restore WasabiBot.slnx

      - name: Install frontend dependencies
        run: npm ci --prefix src/WasabiBot.Api

      - name: Build frontend CSS
        run: npm run build:css --prefix src/WasabiBot.Api

      - name: Test
        run: |
          if [ "${{ matrix.suite }}" = "unit" ]; then
            dotnet run --project test/WasabiBot.UnitTests/WasabiBot.UnitTests.csproj -c Release --no-restore
          else
            dotnet run --project test/WasabiBot.IntegrationTests/WasabiBot.IntegrationTests.csproj -c Release --no-restore
          fi

      - name: Summarize tests
        run: echo "::notice title=CI Tests::${{ matrix.suite }} test suite completed"
