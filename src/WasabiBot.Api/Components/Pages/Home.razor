@page "/"

<PageTitle>@(IsAuthenticated ? $"Dashboard - {DisplayName}" : "Sign In") - Wasabi Bot</PageTitle>

@if (!IsAuthenticated)
{
    <section id="unauthenticated" aria-labelledby="unauthenticated-heading">
        <article>
            <header>
                <h1 id="unauthenticated-heading">Wasabi Bot</h1>
            </header>
            <p>Connect your Discord account to generate a token for the Wasabi Bot API. Once you have a token, you can use it to authenticate API requests.</p>
            <footer>
                <a id="login-link" href="/auth/login-discord" role="button" class="secondary" autofocus>Sign in with Discord</a>
            </footer>
        </article>
    </section>
}
else
{
    <section id="authenticated">
        <article>
            <hgroup>
                <h2>Hello, <span id="user-greeting">@DisplayName</span></h2>
            </hgroup>
            <TokenGenerator />
            <p>Use your token to authenticate API requests. You can explore all available endpoints and test them in the <a href="/scalar/v1" target="_blank" rel="noopener noreferrer">interactive API documentation</a>.</p>
        </article>
    </section>
}

